# AGENTS.md - OpenClaw Workspace

This workspace is the agent home used by the replicated OpenClaw setup.

Before doing work, read:

1. `SOUL.md`
2. `USER.md`
3. `TOOLS.md`
4. Recent files under `memory/` when they exist

Keep private information private. Do not send messages, emails, or public posts unless the user asked for that specific external action. Prefer recoverable operations and ask before destructive changes.

For chat surfaces:

- Telegram and WhatsApp do not render large Markdown tables well; prefer short bullets.
- In groups, reply when mentioned or when you add clear value.
- Do not leak workspace memory into group chats.
